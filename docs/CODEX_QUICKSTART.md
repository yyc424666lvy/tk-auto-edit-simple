# Codex Quick Start

## 你提供

- `1.mp4`
- 可选 `2.mp4`
- `product_brief.json`

## 给 Codex 的一句话

> 读取本仓库 `SKILL.md` 与 `prompts/codex_planner.md`。使用冻结的 `tk-auto-edit-simple` 工作流，不修改 Montaj、portable FFmpeg 8.1.2 或 `workflow/run_workflow.py`。根据 1–2 个商品视频和 product_brief 生成 B Result First 与 C Product Logic 两条视频，并报告 STATUS / OUTPUT_B / OUTPUT_C / RUNTIME / LIMITATION。

Codex 如果拥有本地文件与 shell 权限，可以自己完成 analyze -> plan -> render。
